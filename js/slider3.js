let myImageSlider = new Swiper('.section-three_examples-box', {
    mousewheel: {
        sensitivity: 1,
    },
    speed: 600,
    grabCursor: true,

    slidesPerView: 1,
    // spaceBetween: 20,
    effect: 'slide',
}
);